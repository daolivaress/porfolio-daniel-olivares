<div
  class="bg-neutral-900 rounded-full flex items-center w-fit px-4 py-1.5 text-sm font-medium backdrop-blur-2xl"
>
  <span class="pulse-dot"></span>
  <p>Open to work</p>
</div>

<style>
  .pulse-dot {
    width: 8px;
    height: 8px;
    background-color: #00ff3c;
    border-radius: 50%;
    margin-right: 13px;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0% {
      transform: scale(1);
      box-shadow: 0 0 0 0 rgba(0, 255, 60, 0.7);
    }
    70% {
      transform: scale(1.02);
      box-shadow: 0 0 0 10px rgba(40, 167, 69, 0);
    }
    100% {
      transform: scale(1);
      box-shadow: 0 0 0 0 rgba(40, 167, 69, 0);
    }
  }
</style>
